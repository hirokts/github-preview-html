# セキュリティ設計・自己点検書

最終更新: 2026-07-31

## 結論と適用範囲

この拡張機能は、GitHub で利用者がすでに閲覧できている HTML ファイルを別タブで描画することだけを目的とします。実装を確認した時点で、次の動作は含まれていません。

- 利用者のデータを外部サーバーへ送信する処理
- 分析、広告、トラッキング、外部 SDK
- GitHub のアクセストークン、Cookie、パスワードの取得・保存
- GitHub 上のコンテンツや設定の変更

ただし、この文書はソースコードの自己点検記録であり、絶対的な安全性や脆弱性が存在しないことを法的・技術的に保証するものではありません。その保証には、独立した第三者によるコード監査および継続的な脆弱性診断が必要です。

## データの流れ

```text
GitHub の HTML ファイル画面
  └─ content.js が画面に表示済みの HTML ソースだけを読む
       └─ background.js が chrome.storage.session に一時保存
            └─ preview.html が新しいタブ内の iframe に描画
                 └─ 読み込み後、chrome.storage.session から削除
```

HTML 本文は GitHub の表示済みページから取得します。拡張機能が GitHub API、Raw URL、または外部プレビューサービスへ HTML 本文を送信することはありません。

## 権限と用途

| 権限 | 用途 | 取得・変更するデータ |
| --- | --- | --- |
| `storage` | プレビューを開くための HTML を一時的に渡す | 表示中の HTML。本プレビュー取得時に削除 |
| `tabs` | プレビュー用の新規タブを開く | タブ作成のみ |
| `https://github.com/*` | GitHub の HTML ファイルページへボタンを追加し、表示済みソースを読む | 対象ページの HTML ソース |
| `https://raw.githubusercontent.com/*` | HTML 内の相対リソースを解決するためのホスト許可 | 拡張機能からの API 呼び出しには未使用 |

## 保護している境界

- コンテンツスクリプトの実行対象は `https://github.com/*` のみです。
- HTML を渡す先は同一拡張機能のバックグラウンド処理とプレビュー画面だけです。
- `storage.local` を使用せず、プレビュー対象を永続保存しません。
- iframe に `sandbox` 属性を付与し、プレビュー HTML を拡張機能の権限・オリジンから分離します。プレビュー内のコードは Chrome 拡張 API へアクセスできません。
- 外部から拡張機能へメッセージを送れる `externally_connectable` は設定していません。
- `eval`、リモート JavaScript の動的読み込み、難読化コードは使用していません。

## 既知の制約・残余リスク

- プレビュー対象の HTML は、利用者がその GitHub リポジトリを信頼している前提です。`sandbox` により拡張機能の権限は渡りませんが、HTML が参照する外部画像・CSS・リンク等のネットワークリクエストは発生し得ます。
- 非公開リポジトリ内の相対 CSS・画像などは、Raw URL への認証を行わないため表示できないことがあります。
- プレビューの読み込み前にタブを閉じた場合、一時データはそのブラウザセッションが終わるまで `storage.session` に残る可能性があります。ブラウザ再起動後には保持されません。
- GitHub の DOM 構造が変更されると、ソース抽出またはボタン表示が動作しなくなる可能性があります。これは可用性の問題であり、権限範囲を広げるものではありません。

## 確認手順

1. `chrome://extensions` でこの拡張機能の「詳細」を開き、サイトへのアクセスが `github.com` に限定されていることを確認する。
2. Chrome DevTools の Network を開き、`Preview HTML` 実行中に拡張機能独自の外部 API 呼び出しがないことを確認する。
3. `chrome://extensions` の Service Worker の DevTools で `chrome.storage.session.get(null)` を確認し、プレビューを開いた後にデータが削除されることを確認する。
4. 更新・配布前に、変更差分をレビューし、`manifest.json` の権限追加・外部通信追加がないことを確認する。

## 配布時の判断

Chrome Web Store へ公開する場合も、この文書だけを根拠に「完全に安全」または「セキュリティ上の問題がない」と表示しないでください。説明文には、データを外部送信しないこと、GitHub 表示中の HTML を一時的にプレビューすること、上記の既知の制約を記載してください。
